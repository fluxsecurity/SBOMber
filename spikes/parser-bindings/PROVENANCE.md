# Evidence Provenance

This branch contains a curated, review-friendly version of the parser spike.

The complete chronological research history remains available on:

- branch: `feature/component-2-usage-reachability`;
- final evidence commit: `2c08ba4`.

Important sequence:

1. `9047c11` — fixed comparison protocol;
2. `97ab1f1` — committed corpus and ground truth;
3. `2d5740b` — verified Candidate B language availability;
4. `e412662` — added Candidate B extraction adapter;
5. `9c2e9c8` — compared parser trees;
6. later commits recorded traversal, hostile-input, packaging, maturity and
   resource-safety results.

The protocol therefore preceded the shared corpus comparison and final
candidate decision. Raw repetitive captures remain on the evidence branch.
This curated branch retains the reusable code, fixtures, interpretations and
critical raw results.

Do not delete the evidence branch until project assessment is complete.
