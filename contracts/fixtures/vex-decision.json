{
  "format": "openvex",
  "investigationState": "under_investigation",
  "decision": "S4-09 project selection, 6 September 2026",
  "specVersion": "0.2.0",
  "validator": "OpenVEX JSON Schema 0.2.0 via jsonschema 4.19.2 (spikes/vex-format/validate.py)",
  "consumer": "Grype 0.112.0 (--vex, vex-add); Trivy 0.70.0 verified as secondary consumer",
  "evidence": "spikes/vex-format/DECISION.md",
  "consumerProbeSubjectModel": "package-level",
  "productionSubjectModel": "unresolved",
  "subjectModelNote": "consumerProbeSubjectModel records what the S4-09 consumer runs actually exercised: the vulnerable package as the VEX product. It is a probe result, not a production selection. productionSubjectModel is unresolved because R5 rule 1 requires the scanned application as product with the package as a versioned subcomponent, and Grype 0.112.0 and Trivy 0.70.0 resolve a statement's product @id against the vulnerable package's own purl on directory and filesystem scans, never consulting the subcomponents array. The R5 shape is schema-valid but inert in both consumers. Blocks the S5 exporter's subject model, not the format selection. Evidence: spikes/vex-format/results/subject-model.jsonl"
}
